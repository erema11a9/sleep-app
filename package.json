{
    "scripts": {
        "dev": "concurrently \"cd backend && uv run main.py\" \"cd frontend && npm run dev\"",
        "prod": "concurrently \"cd backend && uv run main.py\" \"cd frontend && npm run build\""
    }
}
